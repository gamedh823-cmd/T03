# 과제3 · 짤·카드 스튜디오 체크리스트

범례: ✅ 확인됨(실제 배포 사이트에서 코드 검토·테스트로 확인) · ⚠️ 문제 발견·사용자 결정 필요 · ☐ 학생 확인 필요

## 카드1 — 편집과 미리보기
- ✅ T03-C01 — 제출한 모든 URL이 로그인·인증·CAPTCHA 없이 새 시크릿 창에서 열린다. *(GitHub Pages는 정적 공개 사이트라 인증 없이 열립니다. curl로 익명 요청 시 200 OK 확인, 소스 저장소 URL도 공개 저장소라 로그인 없이 열림을 확인)*
- ✅ T03-C03 — 이미지 편집 도구가 화면에 보인다. *(1440px 데스크톱 화면에서 레이아웃 정상 표시 확인)*
- ✅ T03-C04 — PNG 파일을 불러올 수 있다.
- ✅ T03-C05 — JPEG 이미지를 불러올 수 있다.
- ✅ T03-C06 — 문구 위치 변경이 미리보기에 즉시 반영된다. *(드래그 시 즉시 redraw 로직 확인)*
- ✅ T03-C07 — 문구 크기 변경이 미리보기에 즉시 반영된다.
- ✅ T03-C08 — 문구 색상 변경이 미리보기에 즉시 반영된다.
- ✅ T03-C09 — 지원하지 않는 파일을 넣으면 거절된다. *(PNG는 통과, GIF는 거부됨을 직접 실행해 확인)*
- ✅ T03-C10 — 지원하지 않는 파일을 넣으면 거부 이유가 표시된다. *(토스트로 "지원하지 않는 파일 형식입니다 (형식명). PNG 또는 JPEG 파일만..." 문구 표시)*

## 카드2 — 화면과 파일의 일치
- ✅ T03-C11 — 1:1 미리보기와 내려받은 파일의 이미지·잘림·위치·줌비율이 일치한다.
- ✅ T03-C12 — 4:5 동일 항목 일치.
- ✅ T03-C13 — 9:16 동일 항목 일치.
  > 실제 배포 사이트에서 세 비율 각각에 그러데이션 배경 + 가장자리에 붙인 문구로 테스트했습니다. "PNG 저장"은 화면에 그려진 캔버스를 그대로 `toDataURL()`로 내보내는 구조라 미리보기와 파일이 원천적으로 같은 데이터입니다 — 세 비율 모두 미리보기 캔버스와 내려받은 파일의 바이트가 완전히 동일하고(문자열 비교 일치), 내려받은 PNG의 실제 픽셀 크기도 600×600 / 600×750 / 540×960으로 정확히 일치함을 코드로 직접 검증했습니다.

## 카드3 — 극단 입력 12건
- ✅ T03-C14 — 긴 한글/영문 혼합, 이모지, 빈 문구, 세로/투명 이미지 등을 포함한 검사 기록 12건이 있다.
- ✅ T03-C15 — 12건 중 최소 1건은 "수정 전 FAIL → 수정 후 PASS" 사례로 남아 있다. *(12번 항목 — 지난 세션에 실제로 고친 파일 검증 버그)*
- ✅ T03-C16 — 잘못된 극단 입력 뒤에도 기존 편집 내용이 예고 없이 사라지지 않는다. *(12건 모두 추가하는 동안 맨 처음 만든 기준 문구 레이어가 끝까지 살아있음을 매 단계 확인)*
  > 실제 배포 사이트에서 12건 전부 직접 실행한 결과입니다.

| # | 입력 내용 | 결과 |
|---|---|---|
| 1 | 긴 한글 문장(약 120자, "가나다라마바사아자차카타파하" 반복) | PASS — 렌더링 정상, 크래시 없음 |
| 2 | 공백 없는 긴 영문(150자) | PASS — 렌더링 정상 |
| 3 | 한글+영문+숫자+특수문자 혼합("Hello세계123!@#₩%^&*()_+한글Mix") | PASS |
| 4 | 이모지 연속 12개 | PASS |
| 5 | 빈 문구("") | PASS — 레이어는 생성되나 크래시 없음 |
| 6 | 공백만 있는 문구("     ") | PASS |
| 7 | 줄바꿈 포함 멀티라인 문구(3줄) | PASS |
| 8 | 최소 폰트 크기(12px 경계값) | PASS |
| 9 | 최대 폰트 크기(160px 경계값) | PASS |
| 10 | 투명(알파 채널) 배경 PNG 이미지 | PASS — 정상 합성 |
| 11 | 세로로 매우 긴 이미지(30×400, 좁고 긴 비율) | PASS — 정상 표시 |
| 12 | 지원하지 않는 파일(.gif) 업로드 시도 — **FAIL→PASS 사례** | **수정 전 FAIL**: 파일 형식 검증 로직이 없어 그대로 이미지 레이어로 추가를 시도해 깨진 레이어가 생성될 위험이 있었음. **수정 후 PASS**: 업로드가 거부되고 "지원하지 않는 파일 형식입니다" 안내가 뜨며 레이어 수가 그대로 유지됨을 확인 |

## 카드4 — 템플릿 관리
- ✅ T03-C17 — 템플릿을 3개 이상 생성할 수 있다. *(3개 생성 후 정상 반영 확인)*
- ✅ T03-C18 — 생성한 템플릿을 다시 불러올 수 있다. *(이전 버전엔 목록을 보여줄 화면 자체가 없어서 불러오기가 불가능했습니다 — "저장된 템플릿" 섹션을 새로 추가하고 불러오기 버튼 동작까지 실제 테스트 완료)*
- ✅ T03-C19 — 생성한 템플릿을 수정할 수 있다. *(불러온 뒤 편집하고 "현재 상태로 덮어쓰기"로 같은 템플릿을 갱신하는 기능을 신규 추가·테스트 완료. 이전 버전은 항상 새 템플릿만 만들 뿐 기존 것을 고치는 방법이 없었습니다)*
- ✅ T03-C20 — 생성한 템플릿을 삭제할 수 있다. *(삭제 버튼 실제 클릭 테스트, 목록에서 사라지고 localStorage에도 반영됨을 확인)*
- ✅ T03-C21 — 템플릿 변경 결과가 새로고침 뒤에도 유지된다. *(localStorage 저장 확인 완료)*

## 카드5 — 옮겨 쓰기와 완성본
- ✅ T03-C22 — 정상 JSON을 가져오면 템플릿이 복원된다. *(`isValidBackupPayload`로 필수 필드까지 검사한 뒤 정상 복원되는 것을 실제 파일로 테스트 완료)*
- ✅ T03-C23 — 문법이 손상된 JSON은 저장 전 거부되고 기존 템플릿이 유지된다. *(`try/catch`로 파싱 실패 시 토스트만 띄우고 기존 templates를 건드리지 않는 것을 코드·테스트로 확인)*
- ✅ T03-C24 — 필수 항목이 빠진 JSON은 저장 전 거부되고 기존 템플릿이 유지된다. *(이전 버전은 `version` 번호만 확인하고 넘어가는 허점이 있었습니다 — `canvasWidth/canvasHeight/background/layers` 필드까지 검사하는 `isValidBackupPayload` 함수를 새로 추가해, 필드가 빠진 JSON을 실제로 넣어 거부되는 것까지 테스트 완료)*
- ✅ T03-C25 — 서로 다른 문구 또는 비율을 가진 완성 이미지가 3개 있다. *([samples/](https://github.com/gamedh823-cmd/T03/tree/main/samples) 폴더에 1:1·4:5·9:16 세 비율, 서로 다른 문구로 3개 추가·push 완료)*
- ✅ T03-C26 — 완성 이미지 3개가 각각 정상적으로 열린다. *(PNG 시그니처와 IHDR 헤더를 직접 디코딩해 600×600 / 600×750 / 540×960 픽셀 크기가 정확히 일치함을 확인)*
- ✅ T03-C27 — 완성 이미지 3개 각각에 저작권 근거가 기록되어 있다. *(배경은 사진이 아니라 canvas API로 직접 생성한 그러데이션이라 전량 본인 저작물입니다 — 외부 이미지 출처 문제 자체가 없습니다. README에 근거 명시)*
- ✅ T03-C28 — 공개 이미지의 위치 정보 메타데이터가 0건이다. *(canvas.toDataURL()로 내보낸 PNG는 EXIF를 애초에 포함하지 않으며, 바이너리 스캔으로 eXIf/GPS 마커 0건 확인)*
- ✅ T03-C29 — 공개 화면과 제출물에 개인정보가 0건이다. *(문서·화면 텍스트에는 개인정보 없음을 확인. 커밋 작성자 이메일(`gamedh823@gmail.com`)이 저장소 커밋 기록에 공개되어 있는 것은 확인했으나, 본인 확인 후 그대로 두기로 결정함)*
- ✅ T03-C30 — 공개 화면·배포 파일·저장소에 비밀값 원본이 0건이다. *(index.html·README·검증문서 전체를 API 키/토큰/비밀번호 패턴으로 검색해 0건 확인. 백엔드가 없는 순수 프런트엔드라 애초에 비밀값을 다룰 일이 없음)*
- ✅ T03-C31 — 짧은 확인 방법에 어디로 가나요 / 3단계 이내 무엇을 하나요 / 무엇이 보이면 통과인지가 구분되어 있다. *([검증안내서.md](검증안내서.md)에 반영 완료)*
- ✅ T03-C32 — 제출문에 AI에게 맡긴 일 / 학생이 직접 판단한 일 / AI 제안을 따르지 않은 일이 구분되어 있다. *([검증안내서.md](검증안내서.md)에 반영 완료)*


