# 과제3 · 짤·카드 스튜디오 (WONWON STUDIO) 검증안내서

## 프로젝트 소개

WONWON STUDIO는 이미지와 문구를 조합해 SNS 콘텐츠를 만들 수 있는 웹 기반 이미지 편집 도구입니다. 사용자는 하나의 캔버스 위에서 사진, 문구, 이모지, 카오모지 같은 요소를 자유롭게 추가하고 배치할 수 있으며, 이렇게 추가된 요소는 각각 독립적인 레이어로 관리되어 이후에도 위치·크기·색상 등을 따로 수정할 수 있습니다. 포토샵 같은 전문 편집 프로그램은 기능이 많은 만큼 처음 쓰는 사람에게는 진입장벽이 높다는 문제가 있는데, WONWON STUDIO는 이런 부담 없이 브라우저만 열면 바로 SNS용 이미지를 만들 수 있도록 레이어 기반 편집 방식을 최대한 단순하게 구현했습니다.

주요 기능은 크게 다섯 갈래로 나뉩니다. 배경·사진 이미지를 불러와 배치하고 크기를 조절하는 이미지 편집, 문구를 추가해 내용·크기·색상·굵기·효과(그림자·외곽선)까지 꾸밀 수 있는 텍스트 편집, 이모지·카오모지를 클릭 한 번으로 얹는 스티커 기능, 여러 레이어의 순서와 위치를 자유롭게 조정하는 레이어 관리, 그리고 작업물을 PNG로 내보내거나 템플릿·JSON 백업으로 저장·복원하는 프로젝트 관리 기능입니다. 조작은 마우스 드래그로 레이어를 옮기고 더블클릭으로 크기 조절 모드에 들어가며, `Delete`로 삭제·`Esc`로 선택 해제하는 방식으로 통일해 별도 설명 없이도 직관적으로 쓸 수 있게 했습니다. 각 기능의 구체적인 사용법은 아래 "전체 사용법 가이드"에 정리되어 있습니다.

기술적으로는 별도 프레임워크 없이 HTML·CSS·JavaScript만으로 작성됐고, 캔버스에 실제로 그림을 그리는 렌더링은 브라우저의 Canvas API를 사용합니다. 저장한 템플릿은 브라우저의 LocalStorage에 남아 새로고침해도 유지되며, 배포는 GitHub Pages로 이루어져 있습니다. 전체가 `index.html` 파일 하나로 구현되어 있어 별도 설치나 서버, 빌드 과정 없이 파일을 열기만 해도 바로 실행되고, Chrome·Edge 등 Chromium 기반 브라우저에서 정상 동작을 확인했습니다.

제작: JIWON YU (기획·디자인·개발) · 교육 목적으로 제작된 프로젝트입니다.

## 공개 결과물 URL
https://gamedh823-cmd.github.io/T03/

## 소스 저장소 URL
https://github.com/gamedh823-cmd/T03/blob/7f73d01a9e1299a64957810bb1fcc7bfb7c1d00f/index.html

## 재현·통과 확인 4가지

**어디로 가나요**
위 공개 결과물 URL로 접속합니다. 로그인·회원가입·CAPTCHA 없이 새 시크릿 창에서 바로 열립니다.

**무엇을 하나요 (3단계 이내)**
1. 왼쪽 "배경 불러오기"로 PNG 또는 JPEG 이미지를 업로드합니다.
2. "T 문구 추가"를 눌러 문구 레이어를 추가하고, 더블클릭으로 크기를 조절합니다.
3. 상단의 "PNG 저장" 버튼으로 결과 이미지를 내려받습니다.

**무엇이 보이면 통과**
업로드한 배경 위에 추가한 문구가 미리보기 화면과 내려받은 PNG 파일에서 같은 위치·크기·색상으로 나타나면 통과입니다.

**안 될 때**
PNG/JPEG/WEBP가 아닌 파일(zip, txt, gif 등)을 넣으면 "지원하지 않는 파일 형식입니다" 안내가 뜨며 업로드가 거부되고, 기존에 작업하던 레이어는 그대로 남아 있습니다.

## 전체 사용법 가이드 (참고용)

위 "재현·통과 확인"은 과제 형식상 3단계로 줄인 최소 확인 절차이고, 여기서는 사이트의 모든 기능을 영역별로 정리했습니다.

### 배경
- **배경 불러오기**: PNG·JPEG·WEBP 이미지를 업로드해 캔버스 배경으로 설정합니다. 그 외 형식은 거부되고 사유가 안내됩니다.
- **배경 확대**: 슬라이더(100~260%)로 확대하거나, 캔버스를 더블클릭할 때마다 25%씩 순환 확대됩니다.
- **배경 위치 이동**: 배경이 있는 상태에서 캔버스를 드래그하면 배경만 이동합니다.
- **배경 제거**: 배경을 지우고 빈 캔버스로 되돌립니다.

### 사진 레이어
- **사진 추가**: 여러 장을 한 번에 선택해 동시에 레이어로 추가할 수 있습니다.
- **이동**: 레이어를 드래그하면 위치가 바뀝니다.
- **리사이즈**: 레이어를 더블클릭하면 모서리에 핸들이 나타나고, 드래그해 크기를 조절합니다(비율 고정).
- **세부 조정**: 왼쪽 "선택한 레이어" 패널에서 이름, 크기(%), 투명도, 모서리 둥글기를 슬라이더로 조절합니다.
- **복제·삭제**: 패널의 버튼 또는 `Delete` 키로 삭제, "복제" 버튼으로 같은 레이어를 하나 더 만듭니다.

### 문구 레이어
- **문구 추가**: 새 텍스트 레이어를 캔버스 중앙에 추가합니다.
- **바로 편집**: 문구 레이어를 더블클릭하면 캔버스 위에서 바로 타이핑할 수 있습니다(`Esc`로 취소, 포커스를 벗어나면 저장).
- **세부 조정**: 왼쪽 패널에서 내용, 크기, 색상, 굵기(Medium~Extra Bold), 효과(없음·그림자·외곽선 + 외곽선 두께/색상), 투명도를 조절합니다.

### 레이어 관리
- **레이어 목록**: 왼쪽 패널에 모든 레이어가 최신순으로 표시되며, 클릭해서 선택합니다.
- **순서 변경**: 맨앞(⇈)·앞으로(↑)·뒤로(↓)·맨뒤(⇊) 버튼으로 쌓이는 순서를 바꿉니다.
- **선택 해제**: `Esc` 키 또는 캔버스 빈 곳 클릭.

### 스티커
- **이모지**: 표정·동물·자연·음식·사물·하트 6개 탭, 클릭 한 번으로 캔버스 중앙에 추가됩니다.
- **카오모지**: 귀여움·동물·인사·하트·리액션 5개 탭, 마찬가지로 클릭 한 번에 추가됩니다.

### 화면비
- 상단 탭에서 1:1(정사각) · 4:5(피드) · 9:16(스토리) · 16:9(와이드) 중 선택하면, 기존 레이어들의 위치·크기가 새 비율에 맞춰 자동으로 다시 계산됩니다.

### 되돌리기 · 다시 실행
- 상단의 되돌리기/다시 실행 버튼, 또는 `Ctrl+Z` / `Ctrl+Shift+Z` 단축키로 최근 50단계까지 이동할 수 있습니다.

### 템플릿 (왼쪽 패널 "저장된 템플릿")
- **저장**: 상단 "템플릿 저장" 버튼으로 현재 캔버스 상태에 이름을 붙여 저장합니다(브라우저 localStorage에 저장되어 새로고침해도 유지).
- **불러오기**: 목록에서 "불러오기"를 누르면 그 템플릿 상태로 캔버스가 바뀝니다.
- **수정**: 템플릿을 불러온 뒤 내용을 편집하고 "현재 상태로 덮어쓰기"를 누르면 같은 템플릿이 갱신됩니다(새 템플릿이 추가로 생기지 않습니다).
- **삭제**: 목록의 "삭제" 버튼으로 제거합니다.

### 백업 (JSON)
- **JSON 내보내기**: 저장된 템플릿 전체 + 현재 작업 상태를 파일로 내려받습니다.
- **JSON 가져오기**: 내보낸 백업 파일을 다시 불러와 복원합니다. 문법이 깨졌거나 필수 항목(캔버스 크기·배경·레이어 정보)이 빠진 파일은 거부되고 기존 데이터는 그대로 유지됩니다.

### 완성본 내보내기
- 상단 "PNG 저장" 버튼으로 현재 캔버스를 이미지 파일(PNG)로 내려받습니다.

## AI와 나의 판단 3줄

**AI에게 맡긴 일**
캔버스 기반 레이어 편집기의 기본 구조(드래그·리사이즈·텍스트 렌더링), 이모지·카오모지 스티커 데이터, 템플릿 저장(localStorage) 로직 초안 작성.

**내가 판단한 일**
화면 전체가 CSS `transform: scale(.70)` 트릭으로 강제 축소되어 있어 글자와 UI가 실제 화면 크기보다 작게 보이고 가독성이 떨어지는 문제를 발견해, 반응형 그리드 레이아웃으로 교체했습니다. 이후 슬라이더·드롭다운 같은 브라우저 기본 UI를 테마에 맞는 커스텀 UI로 다듬고, 파일 업로드 버튼을 드롭존 형태로 바꾸는 등 디자인을 여러 차례 다시 다듬었습니다(포인트 컬러는 검토 끝에 원래의 블루로 확정).

코드를 검토하며 통과 기준 미달 사항 세 가지도 발견해 직접 고쳤습니다: (1) PNG/JPEG 외 파일을 걸러내는 검증 로직이 아예 없던 것(T03-C09/C10), (2) 템플릿을 저장하는 함수는 있지만 저장된 템플릿을 보여주고 불러오거나 수정·삭제할 수 있는 화면 자체가 없었던 것(T03-C17~C21), (3) JSON 가져오기가 버전 번호만 확인하고 필수 필드 누락은 걸러내지 못했던 것(T03-C24). 디자인을 바꾸는 과정에서도 세 가지 모두 다시 실행해 정상 동작을 재확인했습니다.

**AI 말을 안 들은 일**
AI가 처음 제안한 웜톤(앰버) 포인트 컬러 리디자인이 마음에 들지 않아 반려했습니다. 기존 파란색 포인트 컬러를 유지하도록 다시 요청했고, 슬라이더·드롭다운 등 세부 UI가 여전히 브라우저 기본 스타일에 머물러 있다는 점을 추가로 지적해 다시 다듬게 했습니다.
